extends ../layout

block content
  .container

    h1.page-header 설문지
    form(action='/answer/#{post._id}', method='POST')
      p
        b by #{post.email}
        | 
        span at #{moment(post.createdAt).format('YYYY-MM-DD HH:mm:ss')} 
        | 
        | (조회수 #{post.read})
      p
        b 설문 제목
        |  :  #{post.title}
        
      label(for='content') 설문내용  
      p.well(id = 'content')= post.content
      form(role="search",action='/uri/blah' method='post').navbar-form.navbar-left

      
      p
        b ------
      each objs in obj
        .form-group
          input(type="checkbox",id = 'check',name='check', value= objs.content)
          label(for='check') #{objs.content}
          
      
      p
        b -----  
            
      each shts in sht
        .form-group
          label #{shts.content}
          input.form-control(type="text", size=25, id =shts.content, placeholder='답변을 입력해 주세요')
      
      .form-group
        label(for='opinion') 기타 의견이 있다면 입력해 주세요
        textarea.form-control(id='opinion', rows='6', name='opinion')   
        input.form-control(type='hidden', id='email', name='email', value=post.email)
        input.form-control(type='title', id='title', name='title', value=post.title)
        input.form-control(type='content', id='content', name='content', value=post.title)
          
      
      div.form-actions
        a.btn.btn-default(href='javascript:window.history.back();') 뒤로 
        | 
        button.btn.btn-primary(type='submit') 답변완료
          
